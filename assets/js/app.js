/**
 * Main entry file for app logic
 */
import 'svgxuse';
import './global/_global';

window.app = {};

// Config
window.app.config = {
  breakpoints: {
    sm: 30,
    md: 40,
    lg: 60,
    xl: 74,
  },
};
